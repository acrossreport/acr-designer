<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="AcrossReportDesigner.Views.ViewerView">
	<DockPanel>
		<!-- =========================
             上部操作エリア
             ========================= -->
		<StackPanel DockPanel.Dock="Top"
                    Orientation="Vertical"
                    Spacing="6"
                    Margin="6">
			<!-- =========================
                 1行目：ファイル指定＋操作（固定）
                 ========================= -->
			<StackPanel Orientation="Horizontal"
            Spacing="6"
            VerticalAlignment="Center">
				<!-- テンプレート -->
				<Button Content="テンプレート"
						Width="70"
						HorizontalContentAlignment="Center"
						Click="BrowseTemplate_Click"/>
				<TextBox x:Name="TemplatePathBox"
						 Width="300"/>
				<!-- データ -->
				<Button Content="データ"
						Width="70"
						HorizontalContentAlignment="Center"
						Click="BrowseData_Click"/>
				<TextBox x:Name="DataPathBox"
						 Width="300"/>
				<!-- 表示 -->
				<Button x:Name="ShowButton"
						Content="表示"
						Width="70"
						HorizontalContentAlignment="Center"
						Click="Show_Click"/>
				<!-- PDF＋フォント埋込 -->
				<Button x:Name="PdfButton"
						Content="PDF"
						Width="70"
						HorizontalContentAlignment="Center"
						Click="Pdf_Click"/>
				<ComboBox x:Name="EmbedFontComboBox"
						  Width="90"
						  SelectedIndex="0"
						  HorizontalContentAlignment="Center">
					<ComboBoxItem Content="組込ON"/>
					<ComboBoxItem Content="組込OFF"/>
				</ComboBox>
				<!-- HTML＋出力形式 -->
				<Button x:Name="HtmlButton"
						Content="HTML"
						Width="70"
						HorizontalContentAlignment="Center"
						Click="HtmlShow_Click"/>
				<Button x:Name="HtmlSaveButton"
						Content="保存"
						Width="70"
						HorizontalContentAlignment="Center"
						Click="HtmlSave_Click"/>
				<ComboBox x:Name="HtmlModeComboBox"
						  Width="90"
						  SelectedIndex="0"
						  HorizontalContentAlignment="Center">
					<ComboBoxItem Content="画像埋込"/>
					<ComboBoxItem Content="CSS形式"/>
				</ComboBox>
			</StackPanel>
			<!-- =========================
                 2行目：FONT選択＋メッセージ（右端まで伸ばす）
                 ========================= -->
			<Grid VerticalAlignment="Center">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<TextBlock Grid.Column="0"
                           Text="フォント"
                           Margin="0,0,12,0"
                           VerticalAlignment="Center"/>
				<ComboBox Grid.Column="1"
                          x:Name="FontComboBox"
                          Width="220"
                          SelectedIndex="0">
					<ComboBoxItem Content="デフォルト"/>
					<ComboBoxItem Content="Noto 明朝"/>
					<ComboBoxItem Content="Noto ゴシック"/>
					<ComboBoxItem Content="Noto 太字明朝"/>
					<ComboBoxItem Content="Noto 太字ゴシック"/>
				</ComboBox>
				<TextBox Grid.Column="2"
                         x:Name="MessageText"
                         Margin="12,0,0,0"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Stretch"
                         Foreground="Red"
                         IsReadOnly="True"
                         BorderThickness="1"
                         Background="White"
                         CaretBrush="Transparent"
                         TextWrapping="Wrap"
                         MinWidth="300"/>
			</Grid>
		</StackPanel>
		<!-- =========================
             プレビュー領域
             ========================= -->
		<ScrollViewer HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
			<Image x:Name="PreviewImage"
                   Stretch="None"/>
		</ScrollViewer>
	</DockPanel>
</UserControl>
